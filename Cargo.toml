[package]
name = "MiniSCF"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
ndarray = "0.17.2"
ndarray-linalg = { version = "0.18.1", features = ["openblas"] }

[dev-dependencies]
